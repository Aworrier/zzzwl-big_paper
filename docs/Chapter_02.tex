\chapter{随机接入网络及其建模}
\label{chapter2: system model}

本章旨在构建全篇论文的研究基础，对相关理论背景、核心机制及系统模型进行系统阐述。章节内容安排如下：首先，回顾现有主流随机接入标准方案，重点分析面向 5G 的概率接入机制与面向非授权频段的载波侦听机制，在总结技术演进路线的同时揭示现有方案的局限性，从而引出本文的研究动机；其次，深入解析连续传输机制 ST 的基本原理，论证其在提升信道利用率方面的内在优势；接着，引入后续性能量化分析所需的核心数学工具，即离散时间休假排队理论；最后，为实现不同协议间性能的公平对比，统一构建包含网络拓扑、信道特性及业务假设的通用系统场景模型，并定义关键性能评估指标。

\section{随机接入标准方案}
\label{sec:standard_schemes}

本节从机理分析与数学抽象的视角，对现有随机接入协议的内在结构进行解构。这里聚焦于协议在 MAC 层的本质运作原理，并建立其与经典随机接入模型之间的对应关系。该抽象框架为后续连续传输机制设计第~\ref{chapter3: SAST }章和第~\ref{chapter4: CSMAST }章，以及 AI 多协议选择第~\ref{chapter5: ai_selector}章提供统一的理论基础。

\subsection{5G NR 竞争式随机接入流程}
\label{subsec:cba_mapping}

\begin{figure}[t]
	\centering
	\includegraphics[width=0.95\linewidth]{figure/chap2/5G NR标准随机接入过程.pdf}
	\caption{5G NR 两步/四步竞争式随机接入流程示意图}
	\label{fig:cba_access}
\end{figure}
5G NR 随机接入过程继承并演进自 4G LTE 随机接入机制，是终端建立无线连接和获取上行资源的关键过程。在典型的基于竞争的随机接入（Contention-Based Random Access, CBRA）中，终端首先通过随机前导码发起接入请求。当随机接入成功后，基站为终端分配专用无线资源，终端随后在连接状态下进行数据传输。
在当前 5G NR 标准中，随机接入主要包含四步随机接入和两步随机接入两种流程。四步接入流程通过多次握手完成前导检测、随机接入响应、连接请求及冲突解决等过程，从而提高接入可靠性；两步接入流程则通过合并部分信令过程，将前导发送与数据传输进行联合设计，以降低接入时延。两种随机接入流程的基本结构如图 \ref{fig:cba_access} 所示。
从接入机理上看，无论是四步接入还是两步接入，其基本结构均可分为两个阶段：（1）竞争建立阶段，终端通过随机接入过程获取上行资源授权；（2）独占传输阶段，终端在被分配的专用资源上进行连续数据传输。第一阶段表现为概率竞争过程，与 Slotted Aloha 具有相似的随机竞争特征；第二阶段则为资源独占传输过程。
因此，该接入过程可以抽象为一种竞争预留模型，记为 $\text{CBA}(q,M)$。其中 $q$ 表示接入阶段的竞争概率，$M$ 表示一次竞争成功后可连续占用的传输时隙数。当 $M=1$ 时，该模型退化为标准 Aloha；当 $M>1$ 时，竞争需要的开销可在多个数据包之间分摊，从而提高单位数据的吞吐性能。
从 MAC 层抽象角度看，5G NR 基于连接的数据传输机制可以视为一种基于竞争建立、独占传输的 Aloha 扩展形式。


\subsection{5G NR 小数据传输（SDT）随机接入机制}
\label{subsec:sdt_mapping}
\begin{figure}[t]
	\centering
	\includegraphics[width=0.95\linewidth]{figure/chap2/5G NR SDT标准随机接入过程.pdf}
	\caption{5G NR 四步/两步 SDT 标准随机接入流程示意图}
	\label{fig:sdt_access}
\end{figure}

在 5G NR 小数据传输（Small Data Transmission, SDT）机制中，终端在无既有连接状态下可通过随机接入过程直接完成短数据上行传输。在四步 SDT 接入流程中，终端通过发送 Msg1 前导码发起随机接入，并在 Msg3 中携带短数据；而在两步 SDT 接入流程中，终端将前导码与数据封装在 MsgA 中一并发送，从而减少信令交互并降低接入时延。两种流程结构如图 \ref{fig:sdt_access} 所示。

尽管信令流程存在差异，但在初始接入阶段，多个终端仍需在离散配置的 PRACH 时隙中独立发起接入请求。
从 MAC 层视角，每个终端在一次接入机会内仅需做出发送或保持静默的二元决策。该行为可抽象为一次伯努利试验，其发送概率记为 $q\in[0,1]$，且不同终端之间的发送行为在统计意义上相互独立。

因此，SDT 随机接入在 MAC 抽象层面可视为一种时隙化概率竞争机制，其结构与经典的 Slotted Aloha 模型一致，即节点在离散时隙中以概率 $q$ 发起发送，当且仅当该时隙内恰有一个节点发送时接入成功。在不考虑物理层调制、编码等实现细节的前提下，5G NR SDT 随机接入的接入结构可在理论上等价映射为 Slotted Aloha。

\subsection{NR-U 中基于 LBT 的信道接入机制}
\label{sec:csma_mapping}

在 NR-U 场景中，终端在非授权频段发送数据前必须执行 LBT 机制。这里假设终端已经完成初始接入并进入 RRC\_CONNECTED 状态，在后续上行数据传输过程中需要遵守 LBT 信道接入规则。根据 3GPP 规范 \cite{3gpp38213}，LBT 机制可分为 Cat 1 至 Cat 4 四种类型，其竞争控制能力逐级增强，其中 Cat 4 引入随机退避与竞争窗口扩展机制，是 NR-U 中最主要的信道接入方式。

在 Cat 4 LBT 机制中，终端在发起传输前首先根据业务 QoS 要求选择信道接入优先级类别（Channel Access Priority Class, CAPC），并确定对应的竞争窗口参数与最大信道占用时间（Maximum Channel Occupancy Time, MCOT）。随后，终端通过能量检测执行信道侦听，并在信道空闲条件下启动随机退避计数。若侦听过程中检测到信道忙，则退避计数冻结并等待信道重新空闲；当退避计数递减至零时，终端获得发送机会，并在 MCOT 约束下完成数据传输。若传输失败，则通过竞争窗口扩展机制增加后续退避范围，以降低再次碰撞的概率。其基本流程如图 \ref{fig:lbt_process} 所示。


% \begin{itemize}
%     \item Cat 1：无需 LBT；
%     \item Cat 2：固定持续时间的信道检测；
%     \item Cat 3：随机退避但无指数扩展；
%     \item Cat 4：带指数退避的随机退避机制。
% \end{itemize}
% 其中，Cat 4 引入了随机退避与竞争窗口扩展，其结构最接近经典 CSMA/CA。因此，本节重点分析 Cat 4 机制的流程如图\ref{fig:lbt_process}所示，即

\begin{figure}[t]
\centering
\includegraphics[width=0.97\linewidth]{figure/chap2/5G NR标准LBT过程.pdf}
\caption{NR-U中基于LBT的信道接入流程}
\label{fig:lbt_process}
\end{figure}

% \begin{enumerate}[label=步骤\arabic*：, leftmargin=*, itemsep=1ex]

% 	\item 确定信道接入优先级与参数。终端在发起传输前，根据业务 QoS 要求选择信道接入优先级类别 CAPC。每个 CAPC 对应一组预定义的 LBT 参数，包括延迟持续时间参数 $m_p$、竞争窗口最小值 $CW_{\min}$、最大值 $CW_{\max}$，以及最大信道占用时间 MCOT。
%     MCOT 受监管约束限制，用于保证与其他系统的公平共存。

% 	\item 竞争窗口更新与退避计数初始化。  
%     终端根据上一轮传输结果更新竞争窗口 $CW$：
%     \begin{itemize}
%         \item 若传输成功或为初始传输，则 $CW = CW_{\min}$；
%         \item 若传输被判定为失败（例如未收到 HARQ-ACK 或检测到冲突），则
%         $CW \leftarrow \min(2CW+1, CW_{\max})$。
%     \end{itemize}
%     随后在整数区间 $[0, CW]$ 内均匀随机选择退避计数值 $N$。
	
% 	\item 执行延迟持续时间检测。  
%     在开始退避计数前，终端必须检测信道在连续时间$T_d = 16\mu s + m_p \times 9\mu s$
%     内保持空闲（典型 5 GHz 配置）。若期间检测到信道忙，则需等待信道重新空闲，并重新执行完整的延迟持续时间检测。

% 	\item 退避计数与 CCA 时隙监听。  
%     终端在 $9\mu s$ 的 CCA 时隙上执行退避计数：
%     \begin{itemize}
%         \item 若信道空闲，则 $N \leftarrow N-1$；
%         \item 若信道忙，则冻结计数器，
%         待信道重新空闲并重新满足步骤 3 的延迟持续时间要求后恢复倒计时。
%     \end{itemize}
%     当 $N=0$ 时，终端获得发送机会。

% 	\item 数据发送与 MCOT 约束。  
%     终端在获得信道后进行数据传输，
%     传输持续时间不得超过该 CAPC 对应的 $T_{mcot,p}$。
%     传输结束后，
%     根据成功或失败结果更新 $CW$，
%     为下一次接入做准备。
% \end{enumerate}


从 MAC 层视角看，Type 4 LBT 与经典 CSMA/CA 在竞争逻辑上具有高度一致性，即二者均通过信道侦听避免冲突，并利用随机退避分散终端发送时机，同时在失败后通过竞争窗口扩展抑制后续冲突概率。因此，在采用时隙化 CCA、理想碰撞信道等理论分析假设的条件下，NR-U 的 LBT 过程可在 MAC 抽象层面等效映射为 CSMA 型随机接入机制。

进一步地，尽管在标准方案中，竞争控制主要是通过指数退避和竞争窗口更新来实现，但已有研究表明，该退避机制在稳态条件下可等效表示为节点以固定概率发起发送的随机过程，即退避机制与概率发送模型之间存在一一对应关系 \cite{2000_BianchiDCF, 2013_DaiSunDCF}。因此，在理论分析中，可将 LBT 竞争行为抽象为基于发送概率的 CSMA 型随机接入模型。该抽象保留了影响系统吞吐量、时延与公平性的主要竞争特征，同时忽略物理层实现细节与跨系统共存策略等因素，从而为后续 CSMA 网络建模与性能分析提供合理基础。

综上，无论是基于连接的接入过程、面向短报文的接入过程，还是基于 LBT 的竞争接入过程，都包含一个共同的结构特征，即随机竞争阶段与后续连续占用阶段之间的衔接关系。后续第~\ref{chapter3: SAST }章和第~\ref{chapter4: CSMAST }章正是在这一抽象认识基础上，分别对两类典型机制中的连续传输策略展开专门建模与分析。为避免与后续章节重复，连续传输机制的具体流程、建模细节与性能分析不在本章展开，仅保留上述概念性引导。

% \section{连续传输机制的基本原理}
% \label{sec:continuous_transmission_principle}
%
%
%
% 传统随机接入协议如 Aloha 与 CSMA，通常采用一次竞争发送一个数据包的工作方式：节点每完成一次发送后都需释放信道，并重新进入竞争或退避过程。该机制在中高负载下会反复消耗竞争时隙，使大量时间开销停留在侦听、退避与握手环节而非有效载荷传输。为降低这部分重复开销，本文引入连续传输机制 ST：节点一旦竞争成功并完成队首分组发送，可在满足协议约束的条件下继续发送后续缓存分组，直到队列清空或触发释放条件，再重新进入竞争过程。
%
% 如图\ref{fig:ra_vs_st} 所示，传统机制下每个数据包通常对应一次独立竞争，竞争开销与数据包数量近似线性增长；而在 ST 机制下，一次竞争成功可支撑多个数据包连续发送，使竞争开销由逐包重复发生转变为按轮次分摊。因此，ST 的核心增益在于降低单位数据包的平均竞争开销，并在不改变随机接入基本竞争框架的前提下提升有效信道利用率与系统吞吐效率。
% \begin{figure}[t]
% 	\centering
% 	\includegraphics[width=0.95\linewidth]{figure/chap2/连续传输机制对比图.pdf}
% 	\caption{传统随机接入与连续传输机制的对比图}
% 	\label{fig:ra_vs_st}
% \end{figure}


\section{系统场景与模型假设}
\label{sec:general_system_model}

\begin{figure}[t]
	\centering
	\includegraphics[width=0.99\linewidth]{figure/chap2/通用系统场景.pdf}
	\caption{通用系统场景示意图}
	\label{fig:system_model}
\end{figure}

本文首先建立一个抽象的上行随机接入系统模型。在该模型中，网络拓扑保持一致，不同接入机制的差异通过节点接入行为进行刻画，从而为后续章节的性能分析提供统一的建模框架。

在该统一建模框架下，本文考虑一个包含 $n$ 个物联网节点和一个中心基站的 5G 上行随机接入系统，如图~\ref{fig:system_model} 所示。所有节点通过共享无线信道向同一接收机发送数据，基站负责信号接收、译码及反馈控制。系统中仅基站具备接收与反馈能力，节点之间不存在显式协调或直接通信机制，各节点基于本地状态独立进行接入决策。
在全文分析中，系统的物理拓扑结构保持一致，各章节之间的差异主要体现在节点侧接入策略与控制逻辑的设计，而非网络结构本身的变化。具体而言：

\begin{itemize}
	
	\item 在第~\ref{chapter3: SAST }章中，基站主要承担集中接收与反馈功能，节点的接入行为采用概率发送与连续传输相结合的策略，系统分析重点在于刻画 SAST 机制的性能特性；

	\item 在第~\ref{chapter4: CSMAST }章中，基站仍作为统一接收实体，而节点接入行为进一步扩展为侦听、退避与发送相结合的竞争式接入过程，系统模型引入载波监听与连续传输机制，用以刻画 CSMAST 机制的动态行为与性能；

	\item 在第~\ref{chapter5: ai_selector}章中，在保持网络拓扑不变的前提下，本文进一步考虑多协议接入场景。节点可在多种接入协议及其参数配置之间进行选择，其接入行为被抽象为可切换的策略集合，从而实现不同协议与参数组合下的性能评估与接入策略选择。

\end{itemize}
在上述系统场景框架下，具体系统假设如下：

\begin{itemize}
	\item 网络拓扑与流量模型：系统由 $n$ 个统计特性相同的同质节点组成，各节点通过共享无线信道向同一基站发送数据。节点之间的数据包到达过程相互独立且同分布。
	
	在第~\ref{chapter3: SAST }章与第~\ref{chapter4: CSMAST }章的理论分析中，为便于开展排队论建模与闭式性能推导，假设每个节点的数据包到达过程服从参数为 $\lambda$ 的伯努利到达过程，其中 $\lambda$ 的单位为“包/时隙”，表示每个节点在每个时隙产生新数据包的概率为 $\lambda$，且不同时隙之间的到达事件相互独立。由此，系统的总到达率可表示为 $\hat{\lambda}=n\lambda$。具有上述统计特性的业务流量可称为均匀到达流量。
	
	该简化假设使得网络行为刻画为离散时间马尔可夫链，从而便于结合休假排队理论进行解析求解。

	然而，实际通信场景中的业务到达过程通常具有明显的非平稳性与多尺度统计特征，仅采用均匀流量模型难以充分反映真实业务行为。因此，在第~\ref{chapter5: ai_selector}章中，本文进一步引入突发型与周期型两类流量模式，并采用统一的流量特征向量
	$\mathbf{x}_{\text{traffic}}=[\hat{\lambda},\ \mathrm{BI}_{\mathrm{net}},\ \mathrm{BI}_{\mathrm{node}},\ \Psi_{\mathrm{net}},\ \Psi_{\mathrm{node}}]$
	对网络流量特征进行刻画。其中三类典型流量形态定义如下：
	\begin{itemize}

		\item 均匀到达流量 $U$：$\mathrm{BI}_{\mathrm{net}}\approx 0$ 且 $\Psi_{\mathrm{net}}\approx 0$，对应近似泊松到达过程；

		\item 突发到达流量 $B$：$\mathrm{BI}_{\mathrm{node}}>0$ 且 $\Psi_{\mathrm{node}}\approx 0$，通常采用 MMPP 或批量到达过程描述事件驱动型集中上报业务；

		\item 周期到达流量 $P$：$\mathrm{BI}_{\mathrm{net}}$较小且 $\Psi_{\mathrm{net}}>0$，用于刻画周期性或同步上报业务。
	\end{itemize}
	上述流量特征的完整定义与计算方法见第~\ref{chapter5: ai_selector}章表~\ref{tab:traffic_feature_def}，相关参数配置与采样策略在该章仿真实验部分给出。

	\item 时隙结构与同步：系统采用离散时间模型，时间轴被划分为连续且等长的时隙。时隙长度被标准化为传输一个固定长度数据包所需的时间。系统假定所有节点保持严格的时钟同步，因此所有数据包的传输只能在每个时隙的起始时刻发起。
	
	\item 信道模型：假设信道为理想无噪声信道，采用标准碰撞信道模型。当且仅当一个时隙内只有一个节点进行传输时，接收机才能成功解码数据包；若多个节点在同一时隙并发传输，则发生信号叠加导致碰撞，所有涉及的数据包均传输失败。
	
	\item 反馈机制：假设基站提供完美且即时的反馈。节点在每个传输时隙结束时，能够立即通过确认或否定确认信令获知该次传输是否成功。分析中忽略反馈信道的传输错误及传播延迟。
	
	\item 数据包与信令开销：假设系统中所有数据包长度固定，每个数据包占用一个数据传输时隙。随机接入过程中产生的控制信令所带来的信令开销在系统分析中单独统计，用于刻画接入过程的信令成本，但不折算为数据传输时隙，也不计入数据吞吐量计算。
	
	\item 缓存队列与网络状态：每个节点配备容量无限大的缓冲区，数据包在缓冲区中遵循先进先出原则。因此数据包不会因缓存溢出而丢失，但可能会在队列中积压。根据节点缓存的积压情况，我们将网络运行状态划分为两类用于后续分析：
	\begin{itemize}
		\item 饱和状态：假设节点缓存始终非空，节点始终有数据包待传。此状态用于推导系统的理论吞吐量极限。
		\item 非饱和状态：节点缓存可能为空。此状态更符合实际流量场景，用于评估系统的稳态时延与实际吞吐量。
	\end{itemize}
\end{itemize}

基于上述通用假设，后续各章将在统一系统场景下展开递进分析。第~\ref{chapter3: SAST }章聚焦 SAST 机制下的吞吐量、时延及信令开销等性能；第~\ref{chapter4: CSMAST }章分析 CSMAST 机制下的吞吐量、公平性等性能；第~\ref{chapter5: ai_selector}章面向多业务与动态负载环境，综合吞吐量、时延、公平性及信令开销，研究多协议智能切换与参数自适应优化策略。

% \section{基于休假排队理论的分析方法}
% \label{sec:vacation_queuing_theory}

% 本节仅给出后续分析所需的抽象数学框架。考虑一个离散时间随机过程，其状态在两类阶段之间交替演化，分别记为忙期 $B$ 与休假期 $V$，相应周期记为
% \begin{equation}
% C = B + V,
% \end{equation}
% 其中 $B,V,C\in\{0,1,2,\ldots\}$ 或 $\{1,2,\ldots\}$，具体取值范围由后续模型设定决定。以下仅讨论这些随机变量的分布表示、变换工具及其与马尔可夫链之间的关系。
% 对任意离散非负整数随机变量 $X\in\{0,1,2,\ldots\}$，其概率质量函数定义为
% \begin{equation}
% p_X(k)=\Pr\{X=k\},\qquad k=0,1,2,\ldots
% \end{equation}
% 相应的概率生成函数定义为
% \begin{equation}
% G_X(z)=\mathbb{E}[z^X]=\sum_{k=0}^{\infty}p_X(k)z^k,\qquad |z|\le 1.
% \end{equation}
% PGF 是本文后续推导的基本工具，其原因在于它能够同时编码分布信息与矩信息。由 PGF 可恢复 PMF，对应的公式为
% \begin{equation}
% p_X(k)=\frac{1}{k!}\left.\frac{\mathrm{d}^k G_X(z)}{\mathrm{d}z^k}\right|_{z=0}.
% \end{equation}
% 若 $G_X(z)$ 在 $z=1$ 处可导，则有
% \begin{equation}
% \mathbb{E}[X]=G_X'(1),
% \end{equation}
% \begin{equation}
% \mathbb{E}[X(X-1)]=G_X''(1),
% \end{equation}
% \begin{equation}
% \mathrm{Var}(X)=G_X''(1)+G_X'(1)-\bigl(G_X'(1)\bigr)^2.
% \end{equation}
% 因此，一旦得到 $B$、$V$、$C$ 或队列长度变量的 PGF，便可进一步得到其均值、方差及更高阶矩。不仅如此， 
% PGF 对和与复合运算尤其方便。若 $X$ 与 $Y$ 相互独立，则
% \begin{equation}
% G_{X+Y}(z)=G_X(z)G_Y(z).
% \end{equation}
% 若 $S=\sum_{i=1}^{N}X_i$，其中 $X_i$ 独立同分布且与 $N$ 相互独立，则
% \begin{equation}
% G_S(z)=G_N\bigl(G_X(z)\bigr).
% \end{equation}
% 因此，当一个随机变量可表示为随机长度区间上的独立增量之和时，其 PGF 往往可写成某种复合形式。这是后续求解忙期内累积量、休假期内累积量以及周期内累积量的基本依据。

% 下面引入嵌入时刻上的队列过程。设 $Q_m$ 和 $P_m$ 分别表示第 $m$ 个忙期起点与终点处的队列长度，$A_{V,m}$ 和 $A_{B,m}$ 分别表示第 $m$ 个休假期与忙期中的到达量，$S_m$ 表示第 $m$ 个忙期中的离开量，则一般地可写为
% \begin{equation}
% Q_{m+1}=P_m+A_{V,m},
% \end{equation}
% \begin{equation}
% P_m=\bigl(Q_m-S_m\bigr)^+ + A_{B,m}.
% \end{equation}
% 若在给定当前状态后，$A_{V,m}$、$A_{B,m}$ 和 $S_m$ 的条件分布仅依赖于当前状态而与更早历史无关，则序列 $\{Q_m\}$、$\{P_m\}$ 或其联合过程构成离散时间嵌入马尔可夫链。此时，休假排队问题可转化为该嵌入马尔可夫链的稳态分布问题。
% 设嵌入马尔可夫链在稳态下满足
% \begin{equation}
% \pi_k=\Pr\{Q=k\},\qquad k=0,1,2,\ldots,
% \end{equation}
% 则其 PGF 为
% \begin{equation}
% G_Q(z)=\sum_{k=0}^{\infty}\pi_k z^k.
% \end{equation}
% 因此，求解稳态 PMF 与求解稳态 PGF 是等价的。实际推导中，通常先利用递推关系、平衡方程或条件分解得到 $G_Q(z)$ 的函数方程，再由其导出 $\pi_k$ 或矩信息。
% 当休假期内部仍包含多个子状态，且这些子状态之间按照有限状态随机规则切换时，可进一步引入吸收马尔可夫链。设休假期的内部状态由瞬态集合组成，其瞬态子转移矩阵记为 $\mathbf{T}$，初始分布记为 $\boldsymbol{\alpha}$，吸收事件对应休假期结束。则休假期长度 $V$ 的 PMF 满足
% \begin{equation}
% \Pr\{V=k\}=\boldsymbol{\alpha}\mathbf{T}^{k-1}(\mathbf{I}-\mathbf{T})\mathbf{1},\qquad k\ge 1,
% \end{equation}
% 其 PGF 为
% \begin{equation}
% G_V(z)=z\,\boldsymbol{\alpha}(\mathbf{I}-z\mathbf{T})^{-1}(\mathbf{I}-\mathbf{T})\mathbf{1}.
% \end{equation}
% 这表明，吸收马尔可夫链可视为求取休假期 PMF 与 PGF 的一种矩阵化方法；而一旦得到 $G_V(z)$，又可继续代回嵌入马尔可夫链的函数方程中，完成更高层随机变量的求解。

% 从方法结构上看，休假排队模型、PGF 和马尔可夫链三者之间的关系可以概括如下：休假排队模型负责给出随机变量的分解结构，嵌入马尔可夫链负责描述关键观测时刻上的状态更新，PGF 负责将分布递推转化为函数方程并提取矩信息；当内部阶段结构较复杂时，吸收马尔可夫链进一步给出忙期或休假期分布的解析表示。后续章节将在这一抽象框架下，根据具体模型定义 $B$、$V$、$Q_m$、$P_m$、$A_{V,m}$、$A_{B,m}$ 和 $S_m$ 的具体形式，并由此完成各类解析推导。

\section{关键性能指标}
\label{sec:performance_metrics}

为了全面评估所提机制的有效性，并与现有标准方案进行公平对比，本章统一定义以下关键性能指标，包括吞吐量、时延、公平性与信令开销四个维度。

吞吐量是衡量系统有效传输能力的核心指标,本文细分为接入吞吐量与数据吞吐量两类：

\begin{itemize}
	\item 接入吞吐量 $\lambda^a_{out}$：定义为单位时间内成功传输的队首数据包或接入请求的平均数量，单位为包每时隙。该指标反映了系统处理并发接入请求的能力。
	
	\item 数据吞吐量 $\lambda^d_{out}$：定义为单位时间内信道成功传输的所有数据包数量的平均值，其中包含连续传输的后续包，单位为包每时隙。该指标衡量系统的有效负载传输效率。
\end{itemize}

时延直接影响用户体验与服务质量，本文区分接入时延与端到端数据包时延：

\begin{itemize}
	\item 接入时延 $D_a$：定义为一个数据包从到达队首开始，到其成功被基站接收所经历的时间，单位为时隙。该指标主要体现竞争接入的等待成本，包含碰撞重传与退避等待时间。在节点视角的休假排队模型中，接入时延等于休假期长度的期望值。
	
	\item 数据包时延 $D_p$：定义为数据包从生成时刻进入缓存队列，直到被成功传输完毕并离开系统的全过程时延，单位为时隙。该指标包含排队等待时间和接入服务时间，直接反映端到端用户体验。
\end{itemize}

在多用户竞争环境中，资源分配的公平性直接影响系统的稳定性与用户满意度。本文采用 Jain 公平性指数 JFI 作为统一的公平性度量：
\begin{equation}
\text{JFI} = \frac{\left(\sum_{i=1}^{n} x_i\right)^2}{n \cdot \sum_{i=1}^{n} x_i^2}
\end{equation}
其中 $x_i$ 表示第 $i$ 个节点在观测时间窗口内获得的资源份额（可以是成功传输的数据包数量、占用的时隙数、或获得的吞吐量）。JFI的取值范围为 $[1/n, 1]$，其中 $1/n$ 表示最不公平情况（仅一个节点占用全部资源），$1$ 表示完全公平（所有节点获得相同资源）。

% 在本文分析中，我们重点关注短期公平性，即短时间观测窗口内的资源分配公平性。这主要有三方面原因：一是连续传输机制允许单个节点连续占用信道，可能导致短期内的不公平现象；二是实时业务对短期时延抖动敏感，长期统计公平性无法反映瞬时服务质量；三是短期公平性能够捕捉协议在动态流量变化下的瞬态行为。第\ref{chapter4: CSMAST }章和第\ref{chapter5: ai_selector}章将采用滑动窗口法计算不同时间尺度下的 JFI，以评估 SAST 与 CSMAST 机制在公平性方面的权衡。

控制信令开销是随机接入系统的重要成本，直接影响能量效率与频谱利用率。本文以 3GPP 标准规范中必须出现的信令交互过程为统计对象，并采用统一归一化口径：将一次必要的上下行信令交互记为 1 个归一化信令单位。

在该口径下，不同协议的信令交互逻辑统一为：
\begin{itemize}
	\item CBA 协议对应如图\ref{fig:cba_access}所示的基于连接的四步随机接入流程，完整接入过程包含 Msg1 至 Msg4 四次必要交互，以及连接配置确认的第五次交互，因此每次接入尝试产生 4 至 5 个归一化信令单位；
	\item SA 协议对应如图\ref{fig:sdt_access}所示的两步 SDT 接入流程。无论单次接入尝试成功或失败，均包含 MsgA 与 MsgB 两次必要交互，因此每次接入尝试都会产生 2 个归一化信令单位；
	\item SAST 协议对应如图3-6所示的改进型两步 SDT 流程。队首包接入阶段沿用两步 SDT 的两次必要交互，接入成功后的连续发送阶段不再重复前导竞争，仅保留必要反馈交互，因此可在多包传输下摊薄单位数据的信令成本。
\end{itemize}
基于上述统一计量规则，本文定义以下两个指标：
\begin{itemize}
	\item 信令开销 $S$：定义为单位时隙内产生的归一化信令单位数，刻画协议控制面资源消耗强度。
	
	\item 信吞比 STR：定义为信令开销与数据吞吐量的比值，用于衡量单位有效传输能力对应的信令代价：
	\begin{equation}
	\mathrm{STR} = \frac{S}{\lambda_d},
	\end{equation}
	其中 $\lambda_d$ 为数据吞吐量（包/时隙）。STR 越小表示在获得同等数据吞吐时所需信令越少，信令效率越高。
\end{itemize}

通过上述四类指标的综合评估，本文能够在吞吐量、时延、公平性与信令开销等多个维度全面刻画所提机制的性能特征，并为后续章节的理论分析与仿真实验提供统一的评价基准。
