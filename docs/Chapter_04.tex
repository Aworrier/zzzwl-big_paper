\chapter{面向CSMA网络的连续传输机制设计与分析优化}
\label{chapter4: CSMAST }

在前一章验证了连续传输机制在时隙 Aloha 网络中的显著增益后，本章进一步将其扩展至具备载波侦听功能的 CSMA 网络，提出 CSMAST 机制。与 Aloha 中统一的时隙结构不同，CSMA 协议中空闲、碰撞与成功传输的持续时长并不等同为 1 ，为理论建模带来了新的挑战。为此，本章首先结合物理层侦听特性重构系统模型与协议流程；随后引入马尔可夫更新过程方法，构建能够刻画非对称状态时长的精细化休假排队模型；最后基于该模型推导饱和状态下的网络吞吐量与公平性指标，揭示 CSMAST 网络环境下特有的性能规律。

\section{CSMAST 系统模型与机制描述}
\label{sec:csmast_system_model}

本章的研究工作构建于第二章\ref{sec:general_system_model}节所述的通用上行网络模型基础之上，并针对 CSMA 协议特有的物理层与 MAC 层交互特性进行具体化假设与扩展。为了探究连续传输机制在CSMA网络中的性能极限，本章假设网络处于饱和状态，即网络中 $n$ 个同质节点的缓冲区始终非空，所有节点均随时准备竞争信道资源。

\subsection{异构时隙结构与信道状态定义}

在本章节所研究的 CSMA 网络中，时间轴仍被划分为连续的离散时隙，时隙长度被定义为信号在网络中的最大传播延迟与硬件侦听处理时间之和。同时，单个时隙也是系统可实现侦听的最小时间单位。与时隙 Aloha 协议中所有传输行为固定占用一个单位时隙不同，CSMA 网路中的信道占用时长由具体传输行为决定，信道状态持续时间呈现出明显的异构特性。具体而言，为了确保信道感知的准确性，节点在任何传输行为（无论是成功发送还是碰撞结束）之后，都需要经过一个时隙的持续侦听以确认信道状态。
当传输碰撞发生时，受信号叠加物理特性与传播延迟影响，节点无法在发送瞬间立即识别碰撞。因此，本文假设在节点侧需要 $x$ 个时隙（$x \ge 1$）来检测到碰撞信号并终止此时无效的传输，这意味着一次失败的竞争将导致信道被无效占用 $x$ 个传输时隙。算上随后的 1 个侦听时隙，碰撞事件的总持续时间为 $\tau_F = x + 1$。
若节点成功抢占信道并完成数据包有效传输，整个流程涵盖数据包发送、信号传播延迟及接收端确认反馈环节，全程耗时 $y$ 个时隙，通常有 $y \ge x$。因此，一次成功的传输事件将占用 $y$ 个传输时隙，算上随后的 1 个侦听时隙，成功事件的总持续时间为 $\tau_S = y + 1$。
综上所述，从信道资源占用的视角来看，系统在时间域上呈现出三种基本的信道状态：
\begin{enumerate}
	\item 空闲状态，记为状态 I，持续时间为 $\tau_I = 1$ 个时隙，表示当前信道无任何节点进行传输，仅包含必要的侦听开销。
	\item 碰撞状态，记为状态 F，持续时间为 $\tau_F = x + 1$ 个时隙，表示多节点并发导致传输失败，信道被无效占用直至碰撞被检测并恢复侦听。
	\item 成功状态，记为状态 S，持续时间为 $\tau_S = y + 1$ 个时隙，表示单节点成功完成数据包传输并收到确认。
\end{enumerate}
这种状态时长的非对称性（即 $\tau_S \neq \tau_F \neq \tau_I$）是 CSMA 与 Aloha 协议的本质区别，也极大地增加了后续理论建模的复杂度。

\subsection{基于连续传输的 CSMA 协议设计}

在此异构时间模型下，本章提出了一种集成连续传输机制的 CSMA 增强机制，即CSMAST。该协议核心设计思路为，以单次竞争成功的代价，换取多次连续传输的竞争开销减免，通过单次信道抢占红利摊薄整体接入成本。其执行流程遵循竞争接入、连续占用、退避重置的闭环逻辑。具体而言，当网络节点侦听到信道处于空闲状态（状态I），或正处于成功事件S、失败事件F的最后一个时隙时，节点会在下一时隙起始时刻，依据预设概率$q\in[0,1]$执行随机发送判决。当判决不发送，则节点将进入下一时隙并等待下一次信号。当节点判断发送，会有两个情况：
\begin{itemize}
	\item \textbf{成功传输以及连续传输}：本次接入的传输过程未遭遇干扰，顺利将队首数据包传输至基站，即信道完整经历状态 S 的前$y$个时隙，基站立即反馈确认消息，节点获得信道临时控制权并进入连续传输阶段。注意，此时因为本节点正在传输，信道处于繁忙状态，其他节点在每个时隙侦听到信道的繁忙状态后就会保持静默。
	为恪守CSMA先听后说原则、保留其他节点接入权，该节点发送后续数据包前仍需执行标准侦听时隙，但是跳过后续随机退避流程，直接以概率1发送缓存下一包，确保参与下一轮必定参会竞争。
	\item \textbf{碰撞冲突以及退避重置}：若队首包发送或连续传输侦听期间，其他节点传输引发碰撞，此时信道进入状态F并经历前$x$个时隙，基站反馈否定确认或无反馈信息报文。所有涉事节点，包括连续传输节点，需立即终止传输，退避回到常规竞争态，在下一信道空闲时隙重新以概率$q$发起接入。
\end{itemize}
相较于传统CSMA，CSMAST未实现物理层绝对无竞争，而是通过概率1连续发送策略，将单次队首包成功传输转化为多包连续传输机会。该机制可将初始空闲等待时长$\tau_I$、碰撞损失时长$\tau_F$分摊至多个数据包，随着连续传输长度增加，单包平均竞争开销降低，理论上可突破传统CSMA吞吐量极限，提升信道利用率。

\subsection{面向CSMAST的休假排队建模}
\begin{figure}[t]
	\centering
	\includegraphics[width=0.9\textwidth]{figure/chap4/csmast_channel.png}
	\caption{CSMAST方案中的信道状态演变示意图。$B_c$、$V_c$和$C_c$分别表示信道的忙期、假期和周期时间。}
	\label{fig:csmast_channel}
\end{figure}

为了对 CSMAST 机制进行系统的定量分析，本节引入离散时间休假排队理论作为建模基础。鉴于本章考虑的是同质饱和网络，单个节点的统计行为特征可以无偏地映射为整个网络的宏观性能。因此，我们将分析视角聚焦于共享信道的状态演变。从信道资源占用的时间轴来看，CSMAST 网络的运行过程可被抽象为信道忙期 $B_c$与信道假期 $V_c$的交替循环。相应地，一个完整的信道周期定义为 $C_c = B_c + V_c$。图 \ref{fig:csmast_channel} 直观展示了 CSMAST 机制下的信道时序演化过程。结合该图，我们将定义两个核心阶段：

\begin{itemize}
	\item \textbf{信道忙期 $B_c$}: 对应于图中连续出现的成功状态 S。期间某一节点成功掌控信道，依托连续传输机制不间断发送数据包。从系统角度看，这是实现有效吞吐服务的状态。
	
	\item \textbf{信道假期 $V_c$}: 对应于图中碰撞状态 F 与空闲状态 I 的随机混合序列。这一阶段表征了系统在上一忙期结束后，网络中各节点为争夺下一次信道控制权所经历的竞争过程。无论是因为发生碰撞进入状态 F，还是因为无节点发送保持状态 I，该时段均为获取下一次成功接入付出的时间成本和信令开销。
\end{itemize}

为了简化后续数学推导中的符号表示，在不引起歧义的情况下，下文将统一使用 $\overline{B}$ 和 $\overline{V}_c$ 分别表示信道忙期与信道假期的统计平均长度。具体的概率分布推导及性能指标计算将在下一节详细展开。


\section{基于休假排队的性能指标分析}
在进行具体推导之前，本节首先定义三个本章节常用到的三个概率。在第\ref{subsec:channel_vacation_analysis}节指出，饱和网络情况下，当 $n$ 较大且 $q$ 较小时，网络中的聚合接入请求数可近似为参数为 $\theta = nq$ 的泊松随机变量。在任意空闲时隙，网络中发起接入请求的节点数量决定了下一时刻的信道状态。我们定义以下三个情况下的三个概率：
\begin{itemize}
	\item $p_0 = e^{-\theta}$：无任何节点发送请求的概率，即信道保持空闲或忙期延续。
	\item $p_1 = \theta e^{-\theta}$：恰有一个节点发送请求的概率，即当前该节点成功传输。
	\item $p_2 = 1 - p_0 - p_1$：有两个及以上节点发送请求的概率，即发生了碰撞时间。
\end{itemize}
后面我们将充分使用这三个概率，在所建立的休假排队模型中详细阐述忙期和假期分布规律。

\subsection{信道忙期$\overline{B}$分析}

信道忙期 $B_c$（为简化符号，本节统一记为 $\overline{B}$）定义为信道被某一节点连续占用并成功传输有效数据包的时间段。
与第三章所述的 SAST 机制类似，CSMAST 中的忙期维持机制同样取决于当前占用节点是否继续发送以及是否有其他节点干扰。
在饱和网络的假设下，当前获得信道控制权的节点缓存永不为空，因此其在完成当前数据包传输后，总是倾向于以概率 1 继续发送下一个数据包。这意味着，忙期的终止并非由发送方主动停止导致，而是唯一地由信道碰撞触发。
具体而言，一个忙期由若干个连续的成功传输状态组成，每个成功状态持续时间为 $\tau_S$。在每一个成功状态结束前的最后一个时隙，即侦听时隙，网络中其余 $n-1$ 个节点会侦听到信道即将变为空闲。根据前述的泊松近似，此时网络中没有任何其他节点发起接入请求的概率为 $p_0 = e^{-\theta}$。

\begin{itemize}
	\item 若无其他节点接入，概率为 $p_0$，当前占用节点将在侦听时隙后无缝开启下一个数据包的传输，信道状态由一个成功状态刷新为新的成功状态，忙期得以延续。
	\item 若有至少一个其他节点发起接入，概率为 $1-p_0$，则会与当前节点的下一次传输形成碰撞。此时，信道状态由成功状态转变为碰撞状态，标志着当前忙期的结束和新假期的开始。
\end{itemize}
基于上述状态转移机制，忙期内连续成功传输的次数本质上服从参数为 $1-p_0$ 的几何分布。考虑到单次成功传输的持续时间固定为 $\tau_S$，我们可以直接推导出信道忙期的期望长度 $\overline{B}$：
\begin{equation}
	\overline{B} = \frac{\tau_S}{1 - p_0} = \frac{\tau_S}{1 - e^{-nq}}.
\end{equation}
进一步地，为了评估系统服务的稳定性及为后续的公平性分析提供依据，我们需要考察忙期长度的二阶矩特性。根据几何分布的方差性质，信道忙期长度的方差 $\sigma^2_B$ 可表示为：
\begin{equation}
	\sigma^2_B = \tau_S^2 \cdot \frac{p_0}{(1 - p_0)^2} = \frac{e^{-nq} \tau_S^2  }{ (1 - e^{nq})^2}.
\end{equation}

该方差表达式揭示了网络负载对信道占用模式的深刻影响：当网络竞争激烈，对应$p_0$较小的情况，忙期频繁被碰撞打断，$B$的长度普遍较短且波动较小；反之，当网络负载较轻，对应$p_0 $较大的情况，一旦某节点抢占信道，极易形成长时间的连续占用，导致忙期长度的方差急剧增大。这种剧烈的波动性正是造成系统短期公平性恶化的诱因。

\subsection{信道假期$V_c$分析}
\begin{figure}[t]
	\centering
	\includegraphics[width=0.95\textwidth]{figure/chap4/markov_model.pdf}
	\caption{描述信道假期内状态转移过程的吸收马尔可夫链模型。其中状态 F 和 I 为可转移态，状态 S 为吸收态。}
	\label{fig:csmast_markov_absorption}
\end{figure}

相较于信道忙期仅由单一成功状态构成，信道假期$V_c$的时序演变更为复杂。在饱和网络中，一个假期总是始于一次导致忙期结束的碰撞事件，随后信道可能在空闲状态与碰撞状态之间进行多次随机跳转，直至某次竞争产生唯一的成功传输，从而宣告假期终结并开启新的忙期。
更棘手地，空闲时长 $\tau_I$ 与碰撞时长 $\tau_F$ 往往并不相等（通常 $\tau_F > \tau_I$），假期的总长度不仅取决于尝试次数，还高度依赖于系统在两种瞬态之间的驻留比例，这使得简单的几何分布模型不再适用。为了精确刻画这一过程的统计特性（均值与方差），我们将信道在假期内的状态演变建模为一个具有吸收态的马尔可夫链。其状态转移关系如图 \ref{fig:csmast_markov_absorption} 所示。
我们首先构建状态空间与转移矩阵。该模型的具体定义如下：



\begin{itemize}
	\item 模型包含三个状态。其中，碰撞状态 F 和空闲状态 I 构成瞬态集合，表示假期仍在持续；成功状态 S 构成唯一的吸收态，一旦进入该状态，意味着竞争成功，假期结束并转入忙期。
	\item 在任意非忙期时隙，即状态 I 的时隙或状态 F 的最后时隙，网络中发起接入请求的节点数决定了下一时刻的状态跳转：
	\begin{enumerate}
		\item 恰有一个节点发起接入，概率为 $p_1 = \theta e^{-\theta}$，此时信道进入成功状态，过程被吸收。
		\item 无任何节点接入，概率为 $p_0 = e^{-\theta}$，此时信道保持静默并进入空闲状态。
		\item 有多个节点同时接入，概率为 $p_2 = 1 - p_0 - p_1$，此时发生碰撞，信道进入碰撞状态。
	\end{enumerate}
\end{itemize}
基于上述转移逻辑，其状态转移概率矩阵 $\mathbf{P}$ 可表示为如下的标准分块形式：
\begin{equation}
	\mathbf{P} = 
	\begin{pmatrix}
		p_2 & p_0 & p_1 \\
		p_2 & p_0 & p_1 \\
		0 & 0 & 1
	\end{pmatrix}
	=
	\begin{pmatrix}
		\mathbf{T} & \mathbf{R} \\
		\mathbf{0} & \mathbf{I}
	\end{pmatrix},
\end{equation}
其中，$\mathbf{T} = \begin{pmatrix} p_2 & p_0 \\ p_2 & p_0 \end{pmatrix}$ 描述了瞬态之间的转移概率，$\mathbf{R} = \begin{pmatrix} p_1 \\ p_1 \end{pmatrix}$ 描述了进入吸收态的概率。
根据马尔可夫吸收理论，核心计算依赖于基本矩阵 $\mathbf{N} = (\mathbf{I} - \mathbf{T})^{-1}$。矩阵$\mathbf{N}$中的元素 $N_{ij}$ 表示过程从瞬态 $i$ 开始，在被吸收之前访问瞬态 $j$ 的期望次数。 在这里，矩阵$\mathbf{N}$的两个下标索引分别按顺序对应状态I 和状态F。
首先计算 $(\mathbf{I} - \mathbf{T})$：
\begin{equation}
	\mathbf{I} - \mathbf{T} = 
	\begin{pmatrix} 1 & 0 \\ 0 & 1 \end{pmatrix} - \begin{pmatrix} p_2 & p_0 \\ p_2 & p_0 \end{pmatrix} 
	= \begin{pmatrix} 1-p_2 & -p_0 \\ -p_2 & 1-p_0 \end{pmatrix}.
\end{equation}
利用 $p_0 + p_1 + p_2 = 1$ 的关系，可得该矩阵的行列式为 $\det(\mathbf{I} - \mathbf{T}) = (1-p_2)(1-p_0) - (-p_0)(-p_2) = 1 - p_0 - p_2 = p_1$。
通过矩阵求逆公式，解得基本矩阵 $\mathbf{N}$为
\begin{equation}
	\mathbf{N} = \frac{1}{p_1} \begin{pmatrix} 1-p_0 & p_0 \\ p_2 & 1-p_2 \end{pmatrix}.
\end{equation}
由于饱和网络中的假期总是由碰撞状态触发，我们重点关注由碰撞状态开始的期望访问次数。$N_{FF} = (1-p_0)/p_1$ 表示平均经历的碰撞次数，$N_{FI} = p_0/p_1$ 表示平均经历的空闲次数。

进一步地，我们可以轻松推导出假期均值 $\overline{V}_c$。
令列向量 $\boldsymbol{\mu} = [\tau_F, \tau_I]^T$ 表示各瞬态的单次驻留时长。从各瞬态出发到达吸收态的平均总时间向量 $\mathbf{F}$ 由 $\mathbf{F} = \mathbf{N} \boldsymbol{\mu}$ 给出。
我们取向量 $\mathbf{F}$ 的第一个分量，对应初始状态 F，即得到信道假期的均值：
\begin{equation}
	\overline{V}_c = [\mathbf{F}]_1 = [\mathbf{N} \boldsymbol{\mu}]_1 = \frac{1-p_0}{p_1}\tau_F + \frac{p_0}{p_1}\tau_I.
\end{equation}
代入 $p_0 = e^{-\theta}$ 和 $p_1 = \theta e^{-\theta}$，整理得闭式解：
\begin{equation}
	\overline{V}_c = \frac{(1 - e^{-nq})\tau_F + e^{-nq}\tau_I}{nq e^{-nq}}.
\end{equation}

\vspace{0.5em}

接下来推导假期方差 $\sigma^2_{V_c}$。
为了分析公平性，还需计算假期长度$V_c$的二阶矩。根据吸收马尔可夫链理论，到达吸收态的总时间的二阶矩向量 $\mathbf{G}$ 满足方程：
\begin{equation}
	\mathbf{G} = \mathbf{N} ( \mathbf{m}_2 + 2 \cdot \text{diag}(\boldsymbol{\mu}) \cdot \mathbf{T} \mathbf{F} ),
\end{equation}
其中 $\mathbf{m}_2 = [\tau_F^2, \tau_I^2]^T$ 为各瞬态驻留时间的二阶矩向量，$\text{diag}(\boldsymbol{\mu})$ 为以 $\boldsymbol{\mu}$ 为对角元素的对角矩阵。
通过矩阵代数运算，并提取向量 $\mathbf{G}$ 的第一个分量 $G_1$，可以得到从状态 F 出发的假期长度二阶原点矩 $\mathbb{E}[V_c^2]$。
最终，信道假期的方差 $\sigma^2_{V_c}$ 由公式 $\sigma^2_{V_c} = G_1 - (\overline{V}_c)^2$ 给出：
\begin{equation}
	\sigma^2_{V_c} \approx \frac{[(1 - p_0)\tau_F + p_0\tau_I]^2}{p_1^2} = (\overline{V}_c)^2.
\end{equation}
这一结果表明，在大规模网络中，假期的分布近似于指数分布或其离散形式几何分布，其标准差与均值处于同一量级。这种较大的波动性意味着不同节点在争夺信道时面临的等待时间具有显著的随机性，是影响系统短期公平性的关键因素。

\subsection{信道周期与节点周期分析}
\label{subsec:cycle_analysis}

基于前文对信道忙期$B_c$和信道假期$V_c$的独立分析，本节进一步推导完整信道周期$C_c$以及节点周期$C$的统计特性。这一推导能够表征系统的时间演化规律，周期长度的方差将直接决定系统的短期公平性性能。

首先分析信道周期$C_c$的均值与方差。根据休假排队模型定义，一个完整的信道周期$C_c$由一个忙期$B_c$和随后的一个假期$V_c$组成，满足关系式$C_c = B_c + V_c$。

在CSMAST机制下，忙期长度取决于当前占用节点的连续发送意愿以及碰撞发生概率，假期长度取决于竞争过程中的状态跳转。假期开始意味着上一次忙期的状态记忆被重置，碰撞会使得所有节点重新参与信道竞争，因此系统平稳情况下，$B_c$与$V_c$属于相互独立的随机变量。

依据概率论中独立随机变量和的基本性质，信道周期的均值$\overline{C}_c$和方差$\sigma^2_{C_c}$可通过各分量的统计量线性叠加计算得到
\begin{equation}
	\overline{C}_c = \overline{B} + \overline{V}_c,
\end{equation}
\begin{equation}
	\sigma^2_{C_c} = \sigma^2_B + \sigma^2_{V_c}.
\end{equation}
式中$\overline{B}$和$\sigma^2_B$在忙期分析环节已经得出，$\overline{V}_c$和$\sigma^2_{V_c}$通过吸收马尔可夫链分析完成推导。该结果完整刻画了信道资源在占用状态与争夺状态之间切换的时间特征。

其次分析节点周期$C$及其方差映射关系。为评估节点间的公平性，我们将分析视角从公共信道切换至单个节点。定义节点周期$C$为某一特定节点两次连续获得信道控制权、成功开启忙期的时间间隔。

在包含$n$个同质节点的饱和网络中，所有节点具备一致的统计特性，传输概率$q$取值相同，信道每一次成功接入对应一个信道周期$C_c$，且以等概率$1/n$归属任意一个节点。对于指定节点而言，成功接入信道的过程可视为对信道周期序列进行概率为$1/n$的稀疏采样。
换言之，一个节点平均需要等待 $n$ 个信道周期才能再次获得发送机会。因此，节点周期的均值 $\overline{C}$ 与信道周期均值满足如下倍数关系：
\begin{equation}
	\overline{C} = n \overline{C}_c.
\end{equation}

进一步地结合更新过程理论中关于点过程稀疏操作的相关结论，当网络节点规模 $n$ 较大且信道周期之间的相关性较弱时，节点周期的方差 $\sigma^2_C$ 与信道周期方差 $\sigma^2_{C_c}$ 之间存在近似关系：
\begin{equation}
	\label{eq:node_cycle_variance}
	\sigma^2_C \approx n^2 \sigma^2_{C_c} - (1 - n)n\overline{C}_c \approx n^2 \sigma^2_{C_c}.
\end{equation}
上式中的近似 $\sigma^2_C \approx n^2 \sigma^2_{C_c}$ 是基于 $n$ 较大时第一项占主导地位而得到的。

该结论表明，节点周期的波动性对应于信道周期，会被网络规模$n$的平方放大。结合前文分析结论，当传输概率$q$取值较小时，信道忙期的方差$\sigma^2_B$数值较大，存在节点长时占用信道的可能，进而导致信道周期方差$\sigma^2_{C_c}$增大，再通过上述方差映射关系剧烈放大节点周期的波动幅度。这种显著的服务间隔波动，是CSMAST机制在提升吞吐量过程中可能牺牲公平性的数学根源，也为下一节推导Jain公平性指数奠定了理论基础。


\section{吞吐量与公平性的性能推导与分析}
\label{sec:csmast_performance}

基于前文构建的休假排队模型及信道与节点周期的统计特性，本节将正式推导 CSMAST 网络在饱和状态下的两个核心性能指标，即网络吞吐量与 Jain 公平性指数。这不仅能够定量评估该机制的传输效率，还能揭示连续传输策略在系统效率与用户公平之间的内在权衡关系。

\subsection{网络吞吐量分析}

网络吞吐量 $\lambda^{net}_{out}$ 定义为单位时间内信道成功传输的有效数据数量，以包/时隙为单位。
在计算该指标时，必须考虑成功状态 S 的特殊性。虽然成功状态持续 $\tau_S$ 个时隙，但其中仅有 $y$ 个时隙用于实际数据包传输，剩余的 1 个时隙被用于必要的信道侦听。因此，信道忙期 $\overline{B}$ 并不能完全代表有效吞吐时间，需引入修正因子 $\frac{\tau_S - \tau_I}{\tau_S} = \frac{y}{y+1}$。
结合前文推导的 $\overline{B}$ 和 $\overline{V}_c$ 表达式，饱和状态下的网络吞吐量可表示为：
\begin{equation}
	\lambda^{net}_{out} = \frac{\tau_S - \tau_I}{\tau_S} \cdot \frac{\overline{B}}{\overline{B} + \overline{V}_c}.
\end{equation}
将 $p_0 = e^{-nq}$ 及 $\overline{B}, \overline{V}_c$ 的闭式解代入上式，经过代数化简，可得关于总负载 $nq$ 的显式函数：
\begin{equation}
	\label{eq:throughput_csmast}
	\lambda^{net}_{out} = \frac{nq(\tau_S - \tau_I)}{nq\tau_S + e^{nq}(1 - e^{-nq})^2 \tau_F + (1 - e^{-nq})\tau_I}.
\end{equation}

基于上述闭式解，我们给出关于 CSMAST 吞吐量极限的定理：

\begin{theorem}[CSMAST 最大网络吞吐量]
	\label{thm:csmast_max_throughput}
	在饱和状态下，CSMAST 的网络吞吐量是关于总负载 $nq$ 的单调递减函数。其理论最大吞吐量在接入概率 $q \to 0$ 时取得，值为：
	\begin{equation}
		\lambda_{max}^{net} = \lim_{nq \to 0} \lambda^{net}_{out} = \frac{\tau_S - \tau_I}{\tau_S + \tau_I}.
	\end{equation}
\end{theorem}

\begin{proof}
	对于单调性，令 $x = nq$ 表示系统总负载。饱和网络吞吐量可表示为 $g(x) = \frac{C x}{x \tau_S + F(x)}$，其中 $C = \tau_S - \tau_I > 0$，分母中的休假期相关项为 $F(x) = \tau_F e^x (1 - e^{-x})^2 + \tau_I (1 - e^{-x})$。
	对 $g(x)$ 求导，其导数的符号完全取决于分子部分的判别式 $T(x) \triangleq F(x) - x F'(x)$。将 $F(x)$ 及其导数代入，并利用基本不等式 $1 - e^{-x} \le x$ 和 $e^{-x} \le 1$（对于 $x > 0$），可以证明在通常的系统参数设定下（即 $\tau_F \ge \tau_I$），判别式满足：$ T(x) \le 0 $
	由于 $g'(x)$ 与 $T(x)$ 同号，因此 $g'(x) \le 0$ 在定义域内恒成立。这证明了 CSMAST 的网络吞吐量是关于总负载 $nq$ 的单调递减函数。
	
	对于最值，利用洛必达法则对式 (\ref{eq:throughput_csmast}) 在 $nq \to 0$ 处求极限。注意到分母中 $e^{nq}(1-e^{-nq})^2 \approx (nq)^2$ 为高阶小量，主要项为 $(1-e^{-nq})\tau_I \approx nq \tau_I$。分子近似为 $nq(\tau_S - \tau_I)$。分子分母约去 $nq$ 后，第一项 $nq\tau_S$ 趋于 0，剩余部分即为极限值 $\frac{\tau_S - \tau_I}{\tau_S + \tau_I}$。
\end{proof}
值得指出的是，本章构建的基于吸收马尔可夫链的休假排队模型具有良好的普适性。CSMAST 机制的核心增益源于其通过连续传输拉长了平均信道忙期 $\overline{B}$。若我们在模型中强制设定节点在完成单个数据包传输后必须释放信道，即令忙期退化为确定性常数 $\overline{B} = \tau_S$，则该模型即可还原为经典 CSMA 协议的性能描述。关于经典 CSMA 在本章节的相同模型框架下的详细推导及最大吞吐量计算，请参见附录\ref{app:classic_csma}。这一理论联系不仅验证了模型的通用性，也为后续的性能对比提供了统一的数学基准。

定理 \ref{thm:csmast_max_throughput} 揭示了 CSMAST 的性能上限。以典型参数配置 $\tau_S=5, \tau_F=3, \tau_I=1$ 为例，CSMAST 的理论极限吞吐量可达 $4/6 \approx 0.667$，而相同参数下的经典 CSMA 仅能达到约 0.518，相比之下增加了约 29.4\%。这证实了 CSMAST 通过连续传输机制，将空闲侦听 $\tau_I$ 和碰撞退避 $\tau_F$ 的时间成本有效分摊到连续的数据包序列中，从而获得显著的吞吐量增益。

\subsection{公平性性能分析}

尽管连续传输机制通过分摊竞争开销显著提升了系统吞吐量，但其一旦接入便持续占用的特性不可避免地对节点间的公平性构成挑战。为了量化这一影响，本节采用短期 Jain 公平性指数 $J_T$ 作为核心评估指标，并基于更新过程理论推导其闭式解。

设 $\lambda_{out, i}^T$ 为节点 $i$ 在给定观测时间窗 $T$ 内的短期归一化吞吐量。Jain 公平性指数定义如下：
\begin{equation}
	J_T = \frac{\left(\sum_{i=1}^n \lambda_{out, i}^T\right)^2}{n \sum_{i=1}^n \left(\lambda_{out, i}^T\right)^2}.
\end{equation}
由于网络中所有节点是同质的，$\lambda_{out, i}^T$ 可视为独立同分布的随机变量。根据大数定律，当节点数 $n$ 较大时，上述定义的期望形式可近似表示为关于随机变量矩的函数：
\begin{equation}
	\label{eq:jain_moment_form}
	J_T \approx \frac{\mathbb{E}[\lambda_{out, i}^T]^2}{\mathbb{E}[(\lambda_{out, i}^T)^2]} = \frac{1}{1 + \frac{\text{Var}(\lambda_{out, i}^T)}{\mathbb{E}[\lambda_{out, i}^T]^2}}.
\end{equation}
因此，求解 $J_T$ 的关键在于推导短期吞吐量的均值 $\mathbb{E}[\lambda_{out, i}^T]$ 和方差 $\text{Var}(\lambda_{out, i}^T)$。

在饱和网络中，节点 $i$ 的成功接入过程构成一个更新过程。令 $N_i(T)$ 表示节点 $i$ 在时间 $T$ 内成功完成的节点周期数（即获得的忙期次数）。短期吞吐量可表示为单位时间内传输的数据总量：
\begin{equation}
	\lambda_{out, i}^T \approx \frac{N_i(T) \cdot \overline{B}}{T}.
\end{equation}
当观测时间 $T$ 充分长,即$T \gg \overline{C}$时，根据更新理论的渐近性质，计数过程 $N_i(T)$ 近似服从正态分布\cite{1996_Gausian}，其均值和方差分别为：
\begin{equation}
	\mathbb{E}[N_i(T)] \approx \frac{T}{\overline{C}}, \quad \text{Var}(N_i(T)) \approx \frac{\sigma^2_C}{\overline{C}^3} T.
\end{equation}
基于此，我们可以直接得到短期吞吐量的统计特征：
\begin{equation}
	\mathbb{E}[\lambda_{out, i}^T] = \frac{\overline{B}}{\overline{C}}, \quad \text{Var}(\lambda_{out, i}^T) = \left( \frac{\overline{B}}{T} \right)^2 \text{Var}(N_i(T)) = \frac{\overline{B}^2 \sigma^2_C}{\overline{C}^3 T}.
\end{equation}
将上述均值和方差代入式 (\ref{eq:jain_moment_form})，经过化简并展开，得到 Jain 公平性指数的闭式表达式：
\begin{equation}
	\label{eq:jain_explicit}
	J_T \approx \frac{1}{1 + \frac{n}{T} \cdot \frac{n^2 q^2 \tau_S^2 e^{nq} + (e^{nq}-1)^2 \tau_F^2 + 2(e^{nq}-1)\tau_F \tau_I + \tau_I^2}{nq \tau_S e^{nq} + (e^{nq}-1)^2 \tau_F + (e^{nq}-1)\tau_I}}.
\end{equation}


\section{仿真结果与分析}
\label{sec:simulation_results}

本节通过蒙特卡洛仿真从三个层面验证前文结论：理论一致性、与经典 CSMA 的对比增益、以及机制层面的性能权衡。仿真平台基于 MATLAB 构建，严格遵循第 4.1 节定义的异构时隙结构与碰撞信道模型。在所有仿真中，网络处于饱和状态，节点仅在侦听到信道可用时才以概率 $q$ 在下一时隙发起接入。

在仿真中，网络吞吐量按
$\hat{\lambda}_{out}^{net}=\frac{N_{succ}(\tau_S-\tau_I)}{T}$
计算，其中 $N_{succ}$ 表示观测窗口内总体成功接入次数，$T$ 为总仿真时长。
短期公平性由各节点短期吞吐量按 Jain 指数标准定义计算，即
$J_T=\frac{\left(\sum_{i=1}^{n}\lambda_{out,i}^{T}\right)^2}{n\sum_{i=1}^{n}(\lambda_{out,i}^{T})^2}$。
其中节点 $i$ 的短期吞吐量按
$\lambda_{out,i}^{T}=\frac{N_{succ,i}(\tau_S-\tau_I)}{T}$
计算，$N_{succ,i}$ 表示该节点在观测窗口内的成功数据包次数。
除非另有说明，本节采用基准参数 $n=100$、$T=10^6$、$x=2$、$y=4$，即 $\tau_I=1,\tau_F=3,\tau_S=5$，并在图注中明确对应的观测窗口。

图 \ref{fig:csmast_perf_q} 展示了在 CSMAST 方案下，网络吞吐量 $\lambda^{net}_{out}$ 与短期 Jain 公平性指数 $J_T$ 随传输概率 $q$ 的变化情况。从图中可以清晰看出，仿真结果与基于休假排队模型推导出的理论曲线高度吻合，从而验证了第 4.3 节理论分析的准确性。具体而言，网络吞吐量 $\lambda^{net}_{out}$ 随接入概率 $q$ 单调递减，并在 $q$ 趋近于 0 时达到理论峰值。根据设定参数，该峰值逼近 $\frac{\tau_S-\tau_I}{\tau_S+\tau_I} = \frac{4}{6} \approx 0.667$。然而，在获得高吞吐量的同时，系统在极小接入概率 $q \to 0^+$ 下的公平性表现较差。这是因为在极稀疏的接入条件下，一旦少数节点成功抢占信道，便会利用连续传输机制长期维持较高传输速率，而其余大多数节点只能处于空闲侦听状态，极少获得发送机会，从而导致明显的服务饥饿与公平性退化。随着 $q$ 适度增加，碰撞概率的上升打破了这种信道垄断，Jain 公平性指数 $J_T$ 迅速回升并接近 1，而此时网络吞吐量虽然有所下降但仍保持在较高水平。这表明 CSMAST 机制能够通过参数调整，在牺牲少量吞吐量的前提下实现较好的吞吐量与公平性权衡。

\begin{figure}[t]
	\centering
	\includegraphics[width=0.8\textwidth]{figure/chap4/throughput_fairness_vs_q.pdf}
	\caption{CSMAST 网络吞吐量 $\hat{\lambda}_{out}^{net}$ 与短期 Jain 公平性指数 $J_T$ 随传输概率 $q$ 的变化（$n=100,\tau_S=5,\tau_F=3,\tau_I=1,T=10^6$）}
	\label{fig:csmast_perf_q}
\end{figure}

为考察网络规模对性能的影响，图 \ref{fig:csmast_throughput_vs_n} 进一步给出了网络吞吐量随节点数量 $n$ 的变化趋势。在固定接入概率与时隙参数下，随着 $n$ 增大，系统总竞争强度上升，碰撞与空闲侦听开销占比提高，导致有效吞吐量整体下降。图中仿真点与理论曲线保持良好一致，说明本文模型不仅能够刻画 $q$ 维度上的变化规律，也能准确预测节点规模扩展下的吞吐量演化趋势。

\begin{figure}[t]
	\centering
	\includegraphics[width=0.75\textwidth]{figure/chap4/thu_vs_n_CSMAST.pdf}
	\caption{CSMAST 网络吞吐量 $\lambda_{out}^{net}$ 随节点数量 $n$ 的变化。（$q=0.001,\tau_S=5,\tau_F=3,\tau_I=1,T=10^6$）}
	\label{fig:csmast_throughput_vs_n}
\end{figure}

图 \ref{fig:comparison_classic} 进一步展示了 CSMAST 与经典 CSMA 方案在吞吐量与公平性二维平面上的性能轨迹对比。经典 CSMA 的性能曲线基于附录\ref{app:classic_csma} 中推导的理论公式，即式 \ref{eq:classic_csma_throughput} 和式 \ref{eq:optimal_q_classic} 绘制，从而保证两者是在完全相同的时隙结构参数 $x, y$ 以及统一分析框架下进行比较。通过遍历传输概率 $q$，可以得到两种方案在不同工作点下的性能包络。对比结果表明，在同等公平性水平下，CSMAST 的吞吐量上限显著高于经典 CSMA。具体而言，经典 CSMA 的最大理论吞吐量约为 0.518，而 CSMAST 可将这一上限提升至 0.667，实现约 28.8\% 的性能增益，并在图中形成明显更高的性能边界。

\begin{figure}[t]
	\centering
	\includegraphics[width=0.8\textwidth]{figure/chap4/throughput_fairness_tradeoff.pdf}
	\caption{CSMAST 与经典 CSMA 方案的吞吐量-公平性性能轨迹对比}
	\label{fig:comparison_classic}
\end{figure}

进一步分析图 \ref{fig:comparison_classic} 可见，当网络处于较低吞吐量区间（即 $\lambda^{net}_{out} < 0.518$，通常对应较大的 $q$）时，两种方案的性能轨迹几乎重合。其根本原因在于，二者在信道假期 $V_c$ 的统计结构上具有高度同质性：在高竞争强度下，信道都会频繁经历空闲与碰撞状态，使得平均假期长度 $\overline{V}_c$ 接近。在这一工作区间内，系统吞吐量的主要差异来源于信道忙期 $\overline{B}$。当竞争较弱（较小 $q$）时，CSMAST 可借助连续传输显著延长 $\overline{B}$，从而形成吞吐量优势；当竞争增强时，频繁碰撞会持续截断其忙期，导致平均忙期逐步回落至接近单包传输时长，机制行为也随之逼近经典 CSMA。与此同时，系统公平性主要由假期波动主导，而两者在假期统计特征上近似一致，因此在相同 $q$ 下呈现出接近的公平性水平，最终表现为图中几乎重叠的轨迹。上述结果说明，CSMAST 在保持协议兼容性与可比公平性的前提下，能够有效突破传统 CSMA 的吞吐量上限。

\section{本章小结}

本章针对载波侦听网络在高负载下的效率瓶颈，提出了基于连续传输的 CSMAST 机制。考虑到 CSMA 协议显著的状态时长异构性，本章引入吸收马尔可夫链理论，构建了精细化的休假排队分析框架，成功推导了饱和状态下网络吞吐量与 Jain 公平性指数的闭式解。理论与仿真分析表明，CSMAST 通过单次竞争、连续传输的策略有效摊销了信道侦听与碰撞开销，在典型配置下相比传统 CSMA 实现了约 28\% 的吞吐量增益；同时，研究还揭示了该机制中特有的吞吐量与公平性权衡规律，验证了其在提升非授权频段频谱利用率方面的优势与调控灵活性。
