\chapter{类型1假期内新包到达过程的PGF推导}
\label{app:proof_lemma1}

本附录推导类型1假期内新到达数据包数量的概率生成函数（PGF）。记被分析的节点为标记节点。在类型1假期的第一个时隙，可能出现如下三种情形，分别记为 C1、C2、C3。

\begin{itemize}
	\item \textbf{C1}：以概率$qe^{-\theta}$，标记节点向接收机发送数据包，其他$n-1$个节点均不请求传输。此时假期只持续一个时隙。在这个时隙内新包到达量服从参数为$\lambda$的伯努利分布，其概率生成函数为$I(z)=1-\lambda+\lambda z$。因此，$A_{V1}$在该情形下的概率生成函数为
	\begin{equation}
		G_{A_{V1}}(z)|C1 = 1 - \lambda + \lambda z
	\end{equation}
	
	\item \textbf{C2}：以概率 $(1-q)\theta e^{-\theta}$，标记节点不传输，其他 $n-1$ 个节点中有且仅有一个节点成功传输。标记节点继续处于假期。在 C2 期间，它将依次经历三个阶段：(1) 一个非竞争的时隙；(2) 另一个节点的忙期；(3) 一个新的类型1假期。因此，$A_{V1}$ 在该情形下的概率生成函数为：
	\begin{equation}
		G_{A_{V1}}(z)|C2 = (1 - \lambda + \lambda z) \times G_{A_{B}}(z) \times G_{A_{V1}}(z)
	\end{equation}
	
	\item \textbf{C3}：以概率 $1-(1-q)\theta e^{-\theta}-qe^{-\theta}$，第一个时隙中无节点成功。标记节点将在下一个时隙继续竞争信道。在 C3 期间，它将经历两个阶段：(1) 一个空闲时隙；(2) 一个类型1假期。因此，$A_{V1}$ 在该情形下的概率生成函数为
	\begin{equation}
		G_{A_{V1}}(z)|C3 = (1 - \lambda + \lambda z) \times G_{A_{V1}}(z)
	\end{equation}
\end{itemize}

基于全概率定律，综合以上三种情形，我们即可得到目标结果。
