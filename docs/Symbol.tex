% 汇总全文中出现的主要数学符号及其含义

\chapter{\heiti{数学符号}}

\begin{longtable}{p{4.0cm}p{11.0cm}}
    \toprule
        \heiti{符号} &\heiti{含义/说明}\\
        \midrule
        \endfirsthead

        \toprule
        \heiti{符号} &\heiti{含义/说明}\\
        \midrule
        \endhead

        \midrule
        \multicolumn{2}{r}{\small（续表）}\\
        \endfoot

        \bottomrule
        \endlastfoot

        $n$ &网络节点总数\\
        $\lambda$ &单节点到达速率\\
        $\hat{\lambda}$ &归一化网络平均到达率\\
        $p_{ne}$ &节点缓存非空概率\\
        $q$ &节点发起接入尝试的概率\\
        $M$ &连续传输长度（连续占用的数据包数量）\\

        $B,\ V,\ C$ &节点忙期/假期/周期长度（$C=B+V$）\\
        $\lambda_{\mathrm{out}},\ \lambda_{\mathrm{out}}^a,\ \lambda_{\mathrm{out}}^d$ &系统吞吐量；接入吞吐量；数据吞吐量\\
        $\Theta,\ \theta,\ \theta_{sa}$ &聚合接入请求总数随机变量；尝试速率；饱和状态下的尝试速率\\
        $\overline{B}_{sa},\ \overline{V}_{c,sa}$ &饱和状态下平均忙期与平均信道假期\\
        $A_B,\ A_V$ &节点在忙期/假期内的新到达包数\\
        $A_{V_1},\ A_{V_0}$ &类型1/类型0假期内的新到达包数\\
        $Q_t,\ P_t,\ Q,\ P$ &队列长度随机变量（时变/稳态）\\
        $G_Q(z),\ G_P(z)$ &队列变量 $Q,P$ 的概率生成函数（PGF）\\
        $G_{A_B}(z)$ &到达过程 $A_B$ 的概率生成函数\\
        $G_{A_{V_1}}(z),\ G_{A_{V_0}}(z)$ &类型1/类型0假期到达过程的概率生成函数\\
        $p_0$ &忙期结束时节点缓存清空概率（第~\ref{chapter3:SAST}章）\\
        $q^*$ & 最优接入概率（按章节区分不同协议）\\

        $B_c,\ V_c,\ C_c$ &信道忙期/假期/周期长度（$C_c=B_c+V_c$）\\
        $\overline{B},\ \overline{V}_c,\ \sigma_B^2,\ \sigma_{V_c}^2$ &信道忙期/假期均值及其方差\\
        $p_0,\ p_1,\ p_2$ & 无节点发送（注意与第~\ref{chapter3:SAST}章的定义不同）；恰一节点发送；至少两节点发送的概率\\
        $x,\ y$ &碰撞检测所需时隙数；成功传输时隙数\\
        $\tau_I,\ \tau_F,\ \tau_S$ &空闲侦听/碰撞失败/成功状态持续时长\\
        $\mathbf{P},\ \mathbf{T},\ \mathbf{R}$ &吸收马尔可夫链转移矩阵及其分块\\
        $\mathbf{N}$ &基本矩阵（$\mathbf{N}=(\mathbf{I}-\mathbf{T})^{-1}$）\\
        $\boldsymbol{\mu},\ \mathbf{F}$ &驻留时长向量及其一阶时间矩向量\\
        $\mathbf{m}_2,\ \mathbf{G}$ &瞬态驻留时间二阶矩向量；总时间二阶矩向量\\
        $N_{FF},\ N_{FI}$ &基本矩阵对应分量，表示吸收前访问状态 $F/I$ 的期望次数\\
        $\eta$ &有效吞吐时间修正因子\\
        $D$ &平均数据包时延\\
        $J_T$ &公平性指标（Jain 指数）\\

        $T$ &仿真或观测总时隙数\\
        $\mathbf{X}_{\mathrm{in}}$ &标准化输入向量\\
        $\mathbf{A},\ X(t)$ &到达矩阵；网络聚合到达序列\\
        $\mathcal{T}_i,\ \Delta t_i^{(k)}$ &节点 $i$ 的到达时刻集合；到达间隔序列\\
        $\mathrm{BI}_{\mathrm{net}},\ \mathrm{BI}_{\mathrm{node}}$ &网络突发度指标；节点突发度指标\\
        $\Psi_{\mathrm{net}},\ \Psi_{\mathrm{node}}$ &网络周期性强度；节点周期性强度\\
        $\mathrm{CV},\ \mathrm{ID}$ &变异系数；离散指数（Index of Dispersion）\\
        $\mathbf{x}_{\mathrm{traffic}},\ \mathbf{x}_{\mathrm{common}}$ &业务流量特征向量；通用网络参数向量\\
        $\mathbf{x}_{\mathrm{proto}},\ \mathbf{m}$ &协议专属参数向量；有效性掩码向量\\
        $\mathbf{y},\ \tilde{\mathbf{y}},\ \hat{\mathbf{y}}$ &性能指标向量；归一化指标向量；模型预测向量\\
        $\hat{y}_k,\ \tilde{y}_k$ &第 $k$ 个任务预测值；第 $k$ 个任务归一化标签\\
        $S(\cdot),\ f_{\phi}(\cdot)$ &统一仿真映射；监督学习性能预测映射\\
        $\mathrm{norm}(\cdot)$ &指标归一化算子\\
        $U(\cdot)$ &综合效用函数\\
        $\mathbf{w},\ w_i,\ \omega_k$ &效用权重向量及其分量；任务权重系数\\
        $d_{\max}$ &统一协议参数向量的最大维度\\
        $a,\ \mathcal{P}$ &候选协议类型标识；候选协议集合\\
        $\mathbf{x}_{\mathrm{proto}}^*$ &最优统一参数配置（包含最优协议类型及其参数）\\
        $\mathcal{X}_a$ &协议 $a$ 的合法参数空间\\
        $L$ &神经网络隐藏层数\\
        $\mathbf{h}^{(\ell)},\ \mathbf{W}^{(\ell)},\ \mathbf{b}^{(\ell)}$ &第 $\ell$ 层隐藏特征；第 $\ell$ 层权重矩阵；第 $\ell$ 层偏置向量\\
        $b_k$ &第 $k$ 个任务输出头的偏置项\\
        $\mathcal{L}_{\text{total}}$ &多任务总损失函数\\
        $\ell_{\beta}(\cdot)$ &Smooth L1 损失函数\\
        $\beta$ &第5章中为 Smooth L1 超参数；附录C中为固定点推导参数\\
        $\text{MSE},\ \text{MAE}$ &均方误差；平均绝对误差\\

        $\mathbf{M}$ &忙期队列演化的马尔可夫吸收链全转移矩阵\\
        $L_t,\ K_t$ &附录B中马尔可夫链状态变量（队列长度状态与忙期保持状态）\\
        $b_{ij}$ &附录B中从瞬态到吸收态的吸收概率\\

        $\alpha$ &忙期开始时队列长度几何分布参数\\

        $a$ &归一化传播时延参数\\
        $\overline{B}_{st},\ \overline{B}_{cl}$ &CSMAST/经典CSMA的平均忙期\\
        $\hat{\lambda}_{\text{out}}^{\text{cl}}$ &经典CSMA在饱和状态下的吞吐量\\
        $W_0(\cdot)$ &Lambert W 函数主支\\

\end{longtable}

